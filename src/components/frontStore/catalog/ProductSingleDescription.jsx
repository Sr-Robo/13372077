import React from 'react';

// E5 (override-null): a descrição padrão do core é substituída pelo
// sistema de tabs (ProductSingleTabs.jsx) — a tab "Descrição" renderiza o
// mesmo conteúdo via <Editor>. Manter este arquivo vazio (retorno null)
// para que o componente do core não apareça duplicado.
export const ProductSingleDescription = () => null;
